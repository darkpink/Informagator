<UserControl x:Class="Acadian.Informagator.Manager.Panels.MachineList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrl="clr-namespace:Acadian.Informagator.Manager.Controls"
             xmlns:cmd="clr-namespace:Acadian.Informagator.Manager.Commands"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBlock Text="Showing Configuration: " />
            <ctrl:SystemConfigurationPicker SelectedItem="{Binding SelectedConfiguration}" Width="200" />
        </StackPanel>
        <Button Grid.Column="1" HorizontalAlignment="Right" Width="42" Margin="4,2,4,2" Command="{x:Static cmd:PanelChangeCommandManager.GoToMachineEdit}">New</Button>
        <DataGrid Grid.Row="2" Grid.ColumnSpan="2" ItemsSource="{Binding Entities}" CanUserAddRows="False" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="1*" />
                <DataGridTextColumn Header="IP" Binding="{Binding IPAddress}" Width="1*" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*" />
                <DataGridTemplateColumn Header="Edit" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Margin="4,2,4,2" Command="{x:Static cmd:PanelChangeCommandManager.GoToMachineEdit}" CommandParameter="{Binding Id}">Edit</Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Delete" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Margin="4,2,4,2">Delete</Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
